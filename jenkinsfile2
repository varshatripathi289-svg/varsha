@Library('jenkins-ansible-shared-library') _

def config = [
    SLACK_CHANNEL_NAME  : '#jenkins-family',
    ENVIRONMENT         : 'prod',
    CODE_BASE_PATH      : '.',
    ACTION_MESSAGE      : 'Production deployment completed successfully',
    KEEP_APPROVAL_STAGE : true,
    GIT_REPO            : 'https://github.com/varshatripathi289-svg/varsha.git',
    PLAYBOOK_NAME       : 'deploy.yml'
]

ansiblePipeline(config)
